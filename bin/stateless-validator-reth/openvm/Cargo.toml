[workspace]

[package]
name = "stateless-validator-reth-openvm"
version = "0.1.0"
edition = "2024"

[dependencies]
# enable features
alloy-primitives = { version = "1.6.1", default-features = false, features = [
    "map-hashbrown",
    "native-keccak",
] }

# ere
ere-platform-openvm = { git = "https://github.com/eth-act/ere", rev = "8295d94b4598ef14e6c35fff3dd4935f60f2f7ff", features = [
    "std",
    "zkvm-accelerator",
] }

# local
stateless-validator-reth = { path = "../../../crates/stateless-validator-reth", features = ["openvm"] }

[profile.release]
codegen-units = 1
lto = "fat"
