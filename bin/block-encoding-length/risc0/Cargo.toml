[workspace]

[package]
name = "block-encoding-length-risc0"
version = "0.1.0"
edition = "2024"

[dependencies]
# ere
ere-platform-risc0 = { git = "https://github.com/eth-act/ere", tag = "v0.2.0", features = ["std"] }

# local
block-encoding-length = { path = "../../../crates/block-encoding-length" }
