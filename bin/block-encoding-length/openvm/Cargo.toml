[workspace]

[package]
name = "block-encoding-length-openvm"
version = "0.1.0"
edition = "2024"

[dependencies]
# ere
ere-platform-openvm = { git = "https://github.com/eth-act/ere", tag = "v0.6.0", features = [
    "std",
] }

# local
block-encoding-length = { path = "../../../crates/block-encoding-length" }
