[workspace]

[package]
name = "stateless-validator-ethrex-zisk"
edition = "2024"
version = "0.1.0"

[dependencies]
# ere
# NOTE: Using `default-features = false, features = ["inputcpy"]` mainly to disable the other
#       feature enabled by default `user-hints`, which this binary doesn't use but introduces lots
#       of dependencies.
ere-platform-zisk = { git = "https://github.com/eth-act/ere", rev = "8295d94b4598ef14e6c35fff3dd4935f60f2f7ff", default-features = false, features = ["inputcpy"] }

# local
stateless-validator-ethrex = { path = "../../../crates/stateless-validator-ethrex", features = [
    "std",
    "zisk",
] }

[features]
cycle-scope = ["ere-platform-zisk/cycle-scope"]

[profile.release]
codegen-units = 1
lto = "fat"
