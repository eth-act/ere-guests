[workspace]

[package]
name = "stateless-validator-ethrex-openvm"
edition = "2024"
version = "0.1.0"

[dependencies]
# ere
ere-platform-openvm = { git = "https://github.com/eth-act/ere", tag = "v0.16.2", features = [
    "std",
    "zkvm-accelerator",
] }

# local
stateless-validator-ethrex = { path = "../../../crates/stateless-validator-ethrex", features = [
    "std",
    "openvm",
] }

[profile.release]
codegen-units = 1
lto = "fat"
